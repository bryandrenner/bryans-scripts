# Gitconfig

## Installation

1. Include the shared gitconfig into your current `~/.gitconfig`.

    ```
    [include]
    path = path/to/gitconfig.gitconfig
    ```

2. Consider removing configuration from your current `~/.gitconfig` that
  duplicates configuration in the shared gitconfig.